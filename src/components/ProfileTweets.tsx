type userHandle = {
  userHandle: string;
};

const ProfileTweets = (userHandle: userHandle) => {
  console.log("userHandle burada profiletweets", userHandle);
  return <div>ProfileTweets</div>;
};

export default ProfileTweets;
